---
slug: "2025-07-17-智能合約開發核心模式-從查詢到日誌的五種交互實踐-2247484731"
title: "智能合約開發核心模式：從查詢到日誌的五種交互實踐"
authors: [w0x7ce]
tags: [微信公眾號]
date: 2025-07-17
description: "从微信公众号导入的文章《智能合約開發核心模式：從查詢到日誌的五種交互實踐》，保留原始排版图并提供本地 OCR 转写。"
---

# 智能合約開發核心模式：從查詢到日誌的五種交互實踐

> 发布于 2025-07-17 00:08:37（微信公众号导出记录）。
>
> 本文来自公众号后台的“导出文章内容”功能。博客正文由导出长图进行本地 OCR 转写，并保留原始排版图用于逐段核对。
>
> 原文链接：[查看原文](http://mp.weixin.qq.com/s?__biz=MzIwMjMyNzIzNw==&mid=2247484731&idx=1&sn=822d43334bfb9a5b0d95045b893898e6&chksm=96e11091a19699877ec625379158bbeb8bedf9652e0aae35d22f3ec76149ccf99a4d3b449fbb#rd)
>
> OCR 转写有效文字约 5270 字；代码、流程图和版式以文末原始排版图为准。

## 正文（本地 OCR 转写）

original wex7ce EI V1ajero 2025年7月17日 00:88 日本
對於智能合的開發者而言,建合約是第一步。真正的術與工程在於設計
其交互模式一合約如何誓愿請求、如何記錄數據、如何保護自身。一個優秀的設
計能钩在Gas成本、键上透明度與系統安全性之間取得精妙的平衡,而構建
出健壮、高效且值得信赖的去中心化愿用。
本指南將通语五個核心的實模式,引遵你親手提作深入理解微簡单的数據請取到安全的日記
鳞,每種模式的设計理念、速用场景及其深速影容。

實线军情:

- RemixIDE:你的線上智能合的陶猪璟填。

- MetaMask:配置高 Sepolia 潮試。

- Sepolia ETH:用於支付交易的Gas 虞用。

- Chainlink

ETH/USD
地
(Sepolla) :
址
0x694AA1769357215DE4FAC081b f1f309aDC325366
模式一:粹只讀模式(TheReader)
核心理念
提供一個無需支付Gas、不會在键上留下任何痕除的整擦镇取通道,是所有DApp前端展示数摊的
基。
實代碼&#58;ViewOnLyConsumer.sol
// SPDx-License-Identifier: MIT
192*8*a Axtpilos eu5eud
import
"echainlink/contracts/src/vo.8/interfaces/Aggregatorv3Interface.sol";
contract Viewonlyconsumer &#123;
Aggregatorv3Interface internal priceFeed;
constructor(address _priceFeedAddress)&#123;
priceFeed = AggregatorV3Interface(_priceFcedAddress);
function getLatestPrice() public view returns (int)(
//解元组以碳取值格
(. Int price, . , ) = priceFeed.latestRoundData();
return price:

### 1.02615epclafTH e

操作指南

## 1.在 Remix 中编握亚部署ViewonlyConsumer.sol 合的,部署時傅入 Chainlink 地

址。

## 2.在“DepLoyed Contracts” 医檬,贴型荒色的 getLatestPrice 接。

3,察:你重立即看到格的整数值,此适程無需Gas,也無交易叠生。
场景分析
最通用龄DApP前端、借表板或键下显本需要向用户展示键上数據,而不想生任何费用的场景。
模式二:狀想變量日模式(TheClerk)
核心理念
将每一次操作的鲜细記额(包括上下文数握和自定差信息)直接喜入合的的状感量中,使其永久存
储在键上。
宽践代碼&#58;StateLoggingConsumer.sol
// SPDx-License-Identifier: MIT
pragna solidity ~0.8.20;
import
echainlink/contracts/src/vo.8/interfaces/Aggregatorv3Interface.sol";
contract StateLoggingconsumer
Aggregatorv3Interface internal priceFeed;
struct LogEntry &#123;
uint256 tinestanp;
int price;
string message;
address querier;
LogEntry[] public queryLogs:
constructor[address _priceFeedAddress] &#123;
priceFeed = AggregatorV3Interface(_priceFeedAddress) :
function logPricewithHessage(string calldata _nessage) public &#123;
//解精元到以發取價格
(,int latestPrice,
)=priceFeed.latestRoundData();
//等日选存入状感是量数组,这是一個高成本操作
u6ousns6oanb
timestamp: block.tinestanp,
price: latestPrice,
nessage: _message,
querler: msg.sender

P

操作指南

## 1.部墨合的。

## 2.在logPricewithMessage 函傲务的入框中,畸入你想记操的信息(如“Initia1

system check").
3,黏整按纽亚璀阅交易。請特别面意预估的高昂Gas觉用。

## 4.交易成功後,损用queryLogs(e)来查看你套入的第一条完整起缘。

場景分析
由於高的Gas成本。腹力免。谨在低频率、橙重要的状態記条(如修改合约開键参款)且
数擦必须供其他合的直接取琦。才可谨慎考虑。
模式三:事件日模式(TheSecurityCamera)
核心理念
使用 Salidity的event 機制,将日作高交易的一部分經滴地記缘下来,供键下服整整和案
引,是行菜的标率健法。
宽践代碼&#58;EventLoggingConsumer.sol
// SPDx-License-Identifier: MIT
pragna solidity ~0.8.26;
import
"@chainlink/contracts/src/vo,8/interfaces/Aggregatorv3Interface.sol";
contract EventLoggingconsumer
Aggregatorv3Interface internal priceFeed;
cvent Pricelog(address indexed user, int price, string message);
constructor(address _priceFeedaddress) &#123;
priceFeed = AggregatorV3Interface(_priceFeedAddress) :
function logPriceEvent(string calldata _nessage) public &#123;
//解情元朗以發取價格
(,int latestPrice,,,) = priceFeed.latestRoundData(1;
//解提事件,高效配额
emit PriceLog(msg.sender, latestPrice, _message);

操作指南

## 1.部署合的。

## 2.在LogPriceEvent函数旁的输入枢中输入你的信息(如“Userdepos1t

confirned* ) 。

## 3.我行交易,然在Rcnix端的交易配综中找到展開“Logs”。

## 4.靓察:你會看到一個被完美解析的日防,其中包含了损用者、格和你的白定差信点。

场景分析
行案福掌。遵用的整乎所有需要留下操作痕踏的增景:代聚樽服、用户操作、系统狀感磨更等。
模式四:混合模式(TheHybrid)
核心理念
同時提供免查的View回数和一個需要支付Gas但會留下口的交易函数,宽现效率與功能性的
完美结合。
實代碼&#58;HybridConsumer.sol
// SPDx-License-Identifier: MIT
pragna solidity ~0.8.20;
import
@chainlink/contracts/src/vo.8/interfaces/Aggregatorv3Interface.sol°;
contract Hybridconsumer &#123;
Aggregatorv3Interface internal priceFeed;
event ActionLogged(address indexed user, int price, string nessage);
constructor(address _priceFeedAddress) (
priceFeed = AggregatorV3Interface( _priceFeedAddress)]:
//模式一:提供免费、即持的查钢
function getLatestPriceViewonly() public view returns (int) &#123;
(, int price, . ,) = priceFeed.latestRoundData();
return price;
//极式三:掌用户软行需要己骤的震键操作聘用
function performActionwithMessage(string calldata _message) public &#123;
(,int latestPrice, ,,) = priceFeed.latestRoundData(1;
emit ActionLogged(nsg.sender, latestPrice, _message) :

操作指南
getLatestPriceVlewonty的免費翼即時,然後再疯行
艘睡胡用
performActionWithMessage来感受需要Gas 亚產生键上口的交易,深刻理解商著如何熔同
工作。
场景分析
遇用最佳實藏。DeFi潢、NFT市場、键逛等惩大多就DApP的首运架横。
模式五:防間控制模式(TheBouncer)
核心理念
為合的的被感功能段立一可意的「守衡:,在函数机行前,通通require 或nodifier 检查
洪用客是否具有特定權限。
實践代碼&#58;AccessControlledConsumer.sol
// SPDx-License-Identifier: MIT
pragna so11dity ~0.8.20;
import “@openzeppelin/contracts/access/0wmable.sol*;
import

- @chainlink/contracts/src/vo.B/interfaces/Aggregatorv3Interface. sol";

contract AccessControlledconsuner is Dwnable &#123;
Aggregatorv3Interface internal priceFeed;
event AdninLog(address indexed adnin, int price, string message);
constructor(address _priceFccdAddress) Ounable(nsg.sender)(
priceFeed = AggregatorV3Interface( _priceFeedAddress)]:
//只有owner才能调用此函数
(. Int latestPrice, . , ) = priceFeed.latestRoundData[1:
cmit AdminLog(msg.sender, latestPrice, _message);

EDA0990

操作指南

## 1.部署合的。

## 2.用部署睿展户(Owner)调用LogAdminMessage,交暴游成功。

## 3.在MetaHask 中 切换到另一個非Owmer 振户。

4,用新服户营试终用LogAdminNcssagc,交易將會失败。规身體验耀限控制的有效性。
场景分析
安全基石。所有需要權限的敬感操作(如更新费率、智停合約、設置白名單等)都必须使用此梗式或
颈似的權限管理模式。
深度探时:所有權的放案(renounceQwnership)
OwnabLe合约遗提供了一個橙端但重斐的功能&#58;renounce0wnership()。
這是什座?當所有害调用此函数,合的的cwner言被设置為零地址(8x8...3)。這是一
包不可逆轉的操作。

- 比喻:這相需於保隐箱的唯一输趾扔进火山,键趾永久消失,保陆箱再也無法被任何人打

用。

- 為什磨要這度做?為了寶现完金的去中心化。通通放囊所有械,刻始人向社区承,他仍

合的没有任何特椎,合的将永遗按代确白主通行,以此建立無需信任的系统。
盈融是什磨?这也盈味著放聚了所有修復和升级的能力。如果末来强现Bug或需要新增功
货,人知介入。

- 结:放至所有稀是一個非常里霜的決定,通常只在合的辉通格密计且旨在完全自治時才考

。翻於大多数项目,等所有标辅移至多答包或DA0治理是更為场受的去中心化路得。

模式名辑
核心理念
经上可造溯
量佳通用场录
Gas
成
本
(调用时)
性
只
免费
免费的数拔查
DApp 前强数展示。
v1
ew
高
(慎用)低、握重要的
状息曼量
等記数直接寡入
高
状態髮量
將記錄直接寫入
日陆
状感纪線。
日諾
合約存
状態記錄。
行業標,乎所有日需
事件日
中等
非常高
通适事件配缘日
通過事件記錄日
求。
陆
誌
混合模式
结合免費查詢和
免費/中等
高(關键操
通用最佳實,複雜DAp
作)
事件日諾
p。
防問控制
在執行前检查調
崇中
安全和管理,保護敏感功
用者權限
能。

## 原始排版图

![智能合約開發核心模式：從查詢到日誌的五種交互實踐：微信公众号导出原始排版图](/img/wechat/2025-07-17-智能合約開發核心模式-從查詢到日誌的五種交互實踐-2247484731/article.webp)
